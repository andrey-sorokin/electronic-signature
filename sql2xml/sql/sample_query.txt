SELECT EMPNO,
       XMLELEMENT(EMP,
                  XMLATTRIBUTES(EMPNO, DEPTNO),
                  XMLELEMENT(NAME, ENAME),
                  XMLELEMENT(JOB, JOB))
  FROM EMPL
 WHERE SAL = (SELECT MAX(SAL) FROM EMPL)